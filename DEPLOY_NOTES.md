# Deployment Notes

- Latest deploy target: Vercel (multi-service flow)
- `date-fns` pinned to `3.6.0` for `react-day-picker@8` peer compatibility
- `.npmrc` enables `legacy-peer-deps` as belt-and-suspenders fallback
